version: '3.8'
services:
  web:
    build: .
    ports:
      - "3000:3000"
    environment:
      - ADMIN_PASSWORD=${ADMIN_PASSWORD}
    volumes:
      - ./data:/app/data
      - ./public:/app/public:ro
    restart: unless-stopped
